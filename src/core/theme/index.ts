import { colors } from "./colors";
import { typography } from "./typography";

export const theme = {
  colors,
  typography,
  spacing: {
    small: 8,
    medium: 16,
    large: 24,
  },
  borderRadius: 8,
};